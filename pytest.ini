[pytest]
; The repo root has a pre-existing (unrelated) __init__.py, which trips up
; pytest's default filesystem-path-based collection when nested test
; packages are involved (a known class of pytest issue). --pyargs sidesteps
; it entirely by resolving via Python's normal dotted-module import instead
; of walking the filesystem tree.
addopts = --pyargs webapp.tests
